apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: legacy-app-monitor
  namespace: legacy-app-ns
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: legacy-app
  endpoints:
  - port: http
    path: /metrics
    interval: 15s